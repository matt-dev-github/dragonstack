const SECONDS = 1000; // measured in milliseconds
const MINUTES = SECONDS * 60;
const HOURS = MINUTES * 60;
const DAYS = HOURS * 24;

const REFRESH_RATE = 5; // units of __

module.exports = {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    REFRESH_RATE
};
